spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:tcp://localhost/~/lental-dream
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #show_sql: true
        #format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: kyo97kr@gmail.com
    password: miqwhooqaxgsqjqo
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true
logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace
server:
  port: 65000
  tomcat:
    max-http-form-post-size: 5MB

app:
    host: http://localhost:65000
---
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  datasource:
    url: jdbc:h2:tcp://localhost/~/lental-dream
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
  config:
    activate:
      on-profile: local

logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace
server:
  port: 65000

app:
  host: http://localhost:65000
---
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:localtest
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace
server:
  port: 65000
  tomcat:
    max-http-form-post-size: 5MB
app:
  host: http://localhost:65000
